# NASA BloomWatch - Python Dependencies
# Minimal set of actually used dependencies

# Core Data Processing (backend)
numpy==1.24.3
pandas==2.0.3

# Geospatial & Satellite Data (backend)
rasterio==1.3.8
shapely==2.0.1
pystac==1.8.3
pystac-client==0.7.2
planetary-computer==1.0.0

# Time Series Analysis (backend)
scipy==1.11.1

# Web Framework (backend)
flask==2.3.2
flask-cors==4.0.0
python-dotenv==1.0.0

# Jupyter Notebooks (optional - only for notebooks/)
matplotlib==3.7.2
seaborn==0.12.2
plotly==5.15.0
folium==0.14.0
jupyter==1.0.0
ipykernel==6.25.0

# Testing (optional - only for development)
pytest==7.4.0
